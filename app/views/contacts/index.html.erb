<div class="">
<div class="">
  <% @contacts.each do |contact| %>
    <p class=""><%= contact.name %></p>
    <p class=""><%= contact.content %></p>
    <p class=""><%= contact.created_at.strftime('%Y/%m/%d %H:%M') %></p>
  <% end %>
</div>
