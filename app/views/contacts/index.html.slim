- provide(:title, "お問わせ履歴")
section.contacts-section.hero.is-light.is-halfheight
  .columns.is-centered
    .column.is-12-mobile.is-10-tablet.is-9-desktop.is-8-widescreen.is-6-fullhd
      .box
        .info-contents
          - if @contacts.exists?
            - @contacts.each do |contact|
              .info-area
                p.contact-content
                  = contact.name
                p.contact-content
                  = simple_format(contact.content)
                p.contact-date
                  = contact.created_at.strftime('%Y/%m/%d %H:%M')
            = paginate @contacts
          - else
            p.none-info
              | お問合せ履歴がありません
            = link_to "タイムラインへ", posts_path, class: "posts-link"
