<% provide(:title, "Profile") %>
<!-- ログインユーザーの情報 -->
<section class="hero is-dark is-halfheight">
  <div class="hero-body">
    <div class="container">
      <div class="columns is-centered">
        <div class="column is-10-desktop is-10-widescreen">
          <div class="box">
            <div class="field has-text-centered">
              <strong>アカウント情報</strong>
            </div>
            <div class="box">
              <div class="field">
                <div class="user-icon-field">
                  <% if current_user.image? %>
                    <%= image_tag current_user.image.url, alt: "ユーザーアイコン" %>
                  <% else %>
                    <%= image_tag avatar_url(current_user), class: "user-image", alt: "ユーザーアイコン" %>
                  <% end %>
                </div>
                <%= form_with scope: :user, url: users_show_url(current_user), action: :update, method: :post do |f| %>
                  <div class="file">
                    <label class="button is-primary is-outlined is-fullwidth">
                      <%= f.file_field :avatar, accept: 'image/jpeg,image/gif,image/png', class: "file-input", onchange: "this.form.submit();" %>
                      <i class="fas fa-upload"></i>&nbsp;&nbsp;アイコン画像の変更
                    </label>
                  </div>
                <% end %>
              </div>
              <div class="field">
                <span class="user-element">アカウント名: <%= current_user.name %></span>
              </div>
              <div class="field">
                <span class="user-element">調理ジャンル: <%= current_user.introduction %></span>
              </div>
              <div class="field">
                <%= link_to edit_user_registration_path do %>
                  <i class="far fa-sun"></i>
                  <span class="link-btn">setting</span>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<!-- 表示するセクション選択 -->
<section class="appear-section-links">
  <span class="appear-posted-section appear-link">posted</span>
  <span class="appear-liked-section appear-link">liked</span>
  <span class="appear-remember-section appear-link">remember</span>
</section>
<!-- ログインユーザーの投稿一覧 -->
<section class="user-posted-section">
  <h2 class="section-title">posted list</h2>
    <div class="user-posted-counter">*あなたの投稿件数 <%= @posts.count %></div>
  <div class="posts-container">
    <div class="posts-contents">
      <% @posts.each do |post| %>
        <div class="post-area">
          <%= link_to post_path(post.id) do %>
            <figure>
              <%= image_tag(avatar_url(post.user), class: "posted-user-icon", alt: "ユーザーアイコン") %>
            </figure>
            <div class="post-area-text">
              <div class="posted-user-name"><%= post.user.name %>(<%= post.user.introduction %>)</div>
              <div class="posted-date"><%= post_time(post.created_at) %></div>
              <div class="post-text">
                <span class="post-title"><%= post.title %></span>
                <span class="post-content"><%= post.post_content %></span>
                <% unless post.avatar.blank? %>
                  <div class="post-pict">
                    <%= image_tag(post_image_url(post), class: "post-image", alt: "投稿画像") %>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</section>
<!-- ログインユーザーのいいねした投稿一覧 -->
<section class="liked-posts-section">
  <h2 class="section-title">liked list</h2>
  <div class="user-liked-post-counter">*あなたがいいねした投稿件数 <%= current_user.liked_posts.count %></div>
  <div class="posts-container">
    <div class="posts-contents">
      <% current_user.liked_posts.each do |post| %>
        <div class="post-area">
          <%= link_to post_path(post.id) do %>
            <figure>
              <%= image_tag(avatar_url(post.user), class: "posted-user-icon", alt: "ユーザーアイコン") %>
            </figure>
            <div class="post-area-text">
              <div class="posted-user-name"><%= post.user.name %>(<%= post.user.introduction %>)</div>
              <div class="posted-date"><%= post_time(post.created_at) %></div>
              <div class="post-text">
                <span class="post-title"><%= post.title %></span>
                <span class="post-content"><%= post.post_content %></span>
                <% unless post.avatar.blank? %>
                  <div class="post-pict">
                    <%= image_tag(post_image_url(post), class: "post-image", alt: "投稿画像") %>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</section>
<!-- rememberした投稿一覧 -->
<section class="remember-posts-section">
  <h2 class="section-title">remember list</h2>
  <div class="user-remember-post-counter">*あなたがrememberした投稿件数</div>
  <div class="posts-container">
    <div class="posts-contents">
    </div>
  </div>
</section>
<%= javascript_pack_tag 'user/show' %>
