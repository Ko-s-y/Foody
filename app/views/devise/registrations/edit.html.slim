- provide(:title, "アカウント編集")
section.hero.is-dark.is-fullheight
  .hero-body
    .container
      .columns.is-centered
        .column.is-12-mobile.is-10-tablet.is-10-desktop.is-9-widescreen.is-8-fullhd
          .box
            .field.has-text-centered
              strong
                | アカウント情報編集
            .box
              = form_with model: @user, url: user_registration_path, local: true do |f|
                = render "devise/shared/error_messages", resource: resource
                .field
                  = f.label :アカウント名, class: "label"
                  .control.has-icons-left
                    = f.text_field :name, autofocus: true, autocomplete: "name", class: "input is-info", placeholder: "(必須)", maxlength: 12
                    span.icon.is-small.is-left
                      i.fas.fa-user
                  p.help.is-danger
                    | *半角英数字6〜12字以内 例)SampleName23
                .field
                  = f.label :調理ジャンル, class: "label"
                  .control.has-icons-left
                    = f.text_field :introduction, autocomplete: "introduction", class: "input is-info", placeholder: "(必須)", maxlength: 10, list: "job-name"
                    datalist#job-name
                      option[value="うどん"]

                      option[value="郷土料理"]

                      option[value="スイーツ"]

                      option[value="寿司"]

                      option[value="蕎麦"]

                      option[value="中華"]

                      option[value="鉄板焼き"]

                      option[value="天ぷら"]

                      option[value="パン"]

                      option[value="ピッツァ"]

                      option[value="焼肉"]

                      option[value="ラーメン"]

                      option[value="イタリア料理"]

                      option[value="韓国料理"]

                      option[value="スペイン料理"]

                      option[value="タイ料理"]

                      option[value="フランス料理"]

                      option[value="メキシコ料理"]

                      option[value="洋食"]

                      option[value="ロシア料理"]

                      option[value="和食"]

                    span.icon.is-small.is-left
                      i.fas.fa-utensils
                  p.help.is-danger
                    | *10字以内 該当する物がなければ入力をお願い致します
                .field
                  = f.label :メールアドレス, class: "label"
                  .control.has-icons-left
                    = f.email_field :email, autofocus: true, autocomplete: "email", class: "input is-info", placeholder: "(必須)"
                    span.icon.is-small.is-left
                      i.fas.fa-envelope
                    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
                      div
                        | 確認しています: 
                        = resource.unconfirmed_email
                    p.help.is-danger
                      | *@を含めて下さい
                .field
                  = f.label :新しいパスワード, class: "label"
                  .control.has-icons-left.has-icons-right
                    = f.password_field :password, autocomplete: "new-password", class: "input is-info", placeholder: "(#{@minimum_password_length}文字以上)"
                    span#pass-display
                      i.far.fa-eye
                    span.icon.is-small.is-left
                      i.fas.fa-lock
                  p.help.is-danger
                    | *変更しない場合は空白のままにして下さい
                .field
                  = f.label :現在のパスワード, class: "label"
                  .control.has-icons-left.has-icons-right
                    = f.password_field :current_password, autocomplete: "current-password", class: "input is-info", placeholder: "(必須)"
                    span#current-display
                      i.far.fa-eye
                    span.icon.is-small.is-left
                      i.fas.fa-lock
                    p.help.is-danger
                      | *変更には現在のパスワードが必要です
                .field
                  = f.submit "更新する", method: :post, class: "button is-info is-fullwidth m-t-10 m-b-10"
                .field
                  = link_to "戻る", :back, class: "button is-light ope-btn"
              .field
                p.help.is-danger
                  | *削除すると全てのデータが失われます。
                p.help.is-danger
                  |  この操作は取り消せません。ご注意ください。
                = button_to "アカウントの削除", registration_path(resource_name), data: { confirm: "本当にこのアカウントを削除しますか？" }, method: :delete, class: "button is-danger is-fullwidth m-t-10 m-b-10"
